import type { JSX } from "react";
import OnboardAccountForm from "@/accounts/OnboardAccountForm";

/**
 * Component that displays the account onboarding view.
 */
const OnboardAccountView = function (): JSX.Element {
  return <OnboardAccountForm />;
};

export default OnboardAccountView;

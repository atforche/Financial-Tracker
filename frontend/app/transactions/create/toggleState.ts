/**
 * Defines the possible toggle states for the CreateTransactionAccountFrame component.
 */
enum ToggleState {
  Debit = "debit",
  Credit = "credit",
  Transfer = "transfer",
}

export default ToggleState;

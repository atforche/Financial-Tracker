/**
 * Indicates whether trend data is grouped by accounting period or date.
 */
type ChartRangeMode = "AccountingPeriod" | "Date";

/**
 * Semantic colors supported by shared charts.
 */
type ChartColor = "primary" | "secondary" | "positive" | "negative" | "neutral";

export type { ChartColor, ChartRangeMode };
